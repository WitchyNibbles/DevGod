# devgod work

This directory holds current execution state that should survive threads and be inspectable in git.

Suggested flow:

- `briefs/` for intake summaries
- `plans/` for planner output
- `tasks/` for executable task packets
- `reviews/` for gate records
- `release/` for rollout and completion state

Keep these files concise and current.
